import "./globals.css";

export const metadata = {
  title: "BE AWARE - Supported Employment Project",
  description: "EU-funded initiative for supported employment and inclusive workplaces",
};

export default function RootLayout({ children }) {
  return children;
}
